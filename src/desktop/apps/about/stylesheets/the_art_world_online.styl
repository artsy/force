iphone-top-margin = 99px
ease-in-out-quart = cubic-bezier(0.770, 0.000, 0.175, 1.000)

#about-section1-images1-left
#about-section1-images1-right
  .about-image-container:nth-child(even) > *
    span(2)

#about-section1-images2-left
  .about-image-container:nth-child(even) > *
    span(2)

.icon-heart
  width 50px
  height @width
  border-radius 50%
  top 10px
  right @top
  position absolute
  color white
  z-index 1
  line-height @width
  text-align center
  background-color purple-color
  transform-style preserve-3d
  transition transform 0.5s, opacity 0.3s
  transform rotateX(-180deg)
  backface-visibility visible
  opacity 0
  &.is-active
    opacity 1
    transform rotateX(0deg)

.about-section1-cta
  margin 0
  span(2)
  text-align center
  float right
  a
    margin-bottom 20px
