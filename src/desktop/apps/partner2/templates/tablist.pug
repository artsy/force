- function transform(section) {
-   var map = { collection: 'works', articles: 'articles' };
-   return map[section] || section;
- }

nav.partner2-tabs.garamond-bordered-tablist
  if sections.length > 1
    each section, index in sections
      if index > 0
        span.garamond-tab-separator
      a.garamond-tab.partner2-route-link(
        href="/#{profile.get('id')}/#{section}",
        class=(section == currentSection ? 'is-active' : 'is-inactive'),
        data-section=section
      )= transform(section)
