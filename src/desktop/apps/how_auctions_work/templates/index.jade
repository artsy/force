extends ../../../components/main_layout/templates/index.jade

block head
  include meta.jade

append locals
  - bodyClass = bodyClass + ' body-no-margins'
  - assetPackage = 'auctions'

block body
  .how-auctions-work-page
    for section, i in sections.sort(function(a, b) { return a.position > b.position })
      include section.jade

    include mobile_cta.jade

    .haw-multi-page.js-multi-page-embed
      //- Rendered client-side
