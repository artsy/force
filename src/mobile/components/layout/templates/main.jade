extends ./scaffold
include ./login-signup

block body
  #content
    #header-content
      block banner
        mixin login-signup("header")
      header
        != stitch && stitch.components.NavBar({ user: sd.CURRENT_USER, flashMessage: sd.FLASH_MESSAGE, notificationCount: sd.NOTIFICATION_COUNT })

    main
      mixin login-signup("header")
      != stitch && stitch.components.NavBar({ user: sd.CURRENT_USER, flashMessage: sd.FLASH_MESSAGE, notificationCount: sd.NOTIFICATION_COUNT })
      block content

    include footer
