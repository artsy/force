unless auction.isAuctionPromo()
  .auction-preview-registration
    if auction.isPreview()
      if user && user.get('registered_to_bid')
        a.avant-garde-box-button.avant-garde-box-button-black.avant-garde-box-button-green
          | You are registered to bid
      else
        p Register now to bid in the auction when it opens.
        a.avant-garde-box-button.avant-garde-box-button-black( href= auction.registerUrl(sd.CURRENT_PATH) )
          | Register to Bid
    else if auction.isClosed()
      a.avant-garde-box-button.avant-garde-box-button-black.avant-garde-box-button-disabled
        | Online Bidding Closed

.auction-inline-metadata
  .auction-clock.js-auction-clock
    include ../../../components/auction_clock/template

include metadata/description
include metadata/event
include metadata/note
include metadata/associated

unless auction.isAuctionPromo()
  .chevron-nav-list
    a( href='/auction-info' )
      | How bidding works
