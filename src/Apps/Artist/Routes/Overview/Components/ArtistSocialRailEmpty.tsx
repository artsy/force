import { EmptyState } from "Components/EmptyState"

export const ArtistSocialRailEmpty: React.FC = () => {
  return (
    <EmptyState title="We’ll update this area when new posts are available." />
  )
}
