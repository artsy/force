describe("useCreateTokenAndSubmit", () => {
  it("sets SUBMISSION_FAILED error if stripe / elements not found", () => {
    //
  })

  it("sets SUBMISSION_FAILED error if stripe element not found", () => {
    //
  })

  it("sets submitting to true", () => {
    //
  })

  describe("creating a stripe token", () => {
    it("creates a sripe token with an element and address", () => {
      //
    })

    it("if an error is returned, it sets a credit card error", () => {
      //
    })
  })

  describe("adding credit card and address mutation", () => {
    it("submits a mutation", () => {
      //
    })

    it("rejects if an error and sets a field error", () => {})
  })

  it("creates a bidder mutation", () => {
    //
  })

  it("tracks registration submitted", () => {
    //
  })

  it("triggers an onSuccess callback", () => {
    //
  })

  it("triggers correct error handlers if error returned", () => {
    //
  })

  it("sets submitting to false at the very end", () => {
    //
  })
})
