describe("Auction2ConfirmRegistrationRoute", () => {
  it("renders correct components", () => {
    //
  })

  it("redirects to sale home if condition met", () => {
    //
  })

  it("redirects to /register if no credit card on file", () => {
    //
  })

  it("returns null if no qualified credit card", () => {
    //
  })

  it("tracks confirmation page view", () => {
    //
  })

  describe("#handleSubmit", () => {
    it("creates a bidder on submit", () => {
      //
    })

    it("tracks registration submitted", () => {
      //
    })

    it("redirects to auction sale home on complete", () => {
      //
    })
  })

  it("closes the modal by redirecting to sale home", () => {
    //
  })
})
