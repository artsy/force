version: "2"
services:
  force:
    command: yarn test
    extends:
      file: build.yml
      service: force
    environment:
      - CI=true
      - CIRCLE_NODE_INDEX
      - CDN_PRODUCTION_URL
      - CDN_STAGING_URL
    env_file: ../.env.test
