extends ../../../components/main_layout/templates/index

append locals
  - assetPackage = 'galleries_institutions2'

block body
  #galleries-institutions2-page.partners-page

    header.galleries-institutions2-header
      h1
        | Browse #{type === 'gallery' ? 'Galleries' : 'Institutions'}

      .galleries-institutions2-right
        a.galleries-institutions2-right-item( href='/#{type}-partnerships' )
          | Partner with Artsy
        if type === 'gallery'
          a.galleries-institutions2-right-item( href='/gallery-insights' )
            | Gallery Insights

    include ../components/primary_carousel/index

    .js-partner-category-carousels
      each carousel in carousels
        - var category = carousel.category
        - var partners = carousel.partners.models
        .partner-category-carousel
          include ../components/partner_cell_carousel/template

    include ./footer
