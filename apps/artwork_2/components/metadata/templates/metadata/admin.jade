if user && user.isAdmin()
  nav.artwork-metadata__admin
    a(
      href='#{artwork.href}/download/#{helpers.metadata.filename(artwork)}.jpg'
      target='_blank'
    )
      i.icon-download
      | Download

    a(
      href='#{sd.CMS_URL}/artworks/#{artwork.id}/edit?current_partner_id=#{artwork.partner.id}'
      target='_blank'
    )
      i.icon-edit
      | Edit

    a(
      href='#{sd.GENOME_URL}/genome/artworks?artwork_ids=#{artwork.id}'
      target='_blank'
    )
      i.icon-link
      | Genome
