.artwork-metadata
  garamond-size('s-body')

  &__lot-number
  &__artist
  &__partner
  &__secondary-metadata
    block-margins(10px)

  &__lot-number
    avant-garde-size('body')

  &__artist
    > .entity-follow
      display block
      // Offset icon spacing from within 32px square
      margin-left -8px

  &__title
    garamond-size('l-body')

  &__secondary-metadata
    color gray-darker-color
