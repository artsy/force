@require '../tabs'

.artwork-artists
  //

.artwork-artist
  &__name
    serif('headline')
    margin 0 0 distance(gutter) 0

  &__content
    &__biography
      serif('s-body')
      a
        fine-faux-underline()
