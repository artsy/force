.artwork-container
  - if artwork.hasDefaultImage()
    .artwork-image-container
      img( src=artwork.defaultImageUrl() )
  .artwork-info-container
    - if artwork.get('artist')
      .artist-name!= artwork.get('artist').name
    h3!= artwork.titleAndYear()
