#partner-overview
  if showBanner
    section.partner-overview-section.partner-overview-messages
      p.message-title #{partner.get('name')} participates in Artsy’s art fair coverage
      p.message-title but does not have a full profile.
      p.message-content A full Artsy profile includes artists, works, shows, and contact information for collectors.
      br
      br
      p.message-content
        | Do you represent #{partner.get('name')}?
        | Learn about
        = ' '
        a.faux-underline( href='/gallery-partnerships' ) Artsy gallery partnerships.

  .partner-overview-container
    for module in layout
      if module.component || module.template
        section.partner-overview-section( data-module=module.name )
          if module.title
            h3.partner-overview-section-title= module.title
          if module.viewAll
            .partner-overview-section-view-all
              a( href=module.viewAll ) View All
          .partner-overview-section-content
            if module.template
              != module.template
            else
              .loading-spinner
