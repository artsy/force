header.partner-header
  .partner-brand-container
    .partner-info
      h1.partner-name= profile.displayName()
      .partner-locations
      .partner-actions
        button.avant-garde-follow-button-black.follow-button.is-small
        if profile.get('follows_count')
          .partner-followers
            = profile.formatFollowText()
    a.profile-badge.partner-icon( href=profile.href() )
      .profile-badge-icon
        if profile.has('icon')
          img( src=profile.iconImageUrl(), alt=profile.displayName() )
        else
          .profile-badge-initials= profile.defaultIconInitials()
  .partner-navs-container
    .partner-nav
