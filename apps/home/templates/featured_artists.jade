for link in artistLinks
  li.home-featured-artist-link
    a( href=link.get('href') )
      .home-featured-cover-img(
        style="background-image: url(" + link.imageUrl('large_rectangle') + ")",
      )
        img( src=link.imageUrl('large_rectangle') )
      h4= link.get('title')
      h5= link.get('subtitle')