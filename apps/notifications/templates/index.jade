extends ../../../components/main_layout/templates/index

block head
  title Works by Artists You Follow | Artsy

append locals
  - assetPackage = 'notifications'
  - bodyClass = bodyClass + ' body-infinite-scroll'

block body
  #notifications-page.main-layout-container
    #notifications-header
      h1 Works by Artists You Follow
    #notifications-filter
      .notifications-sub-header Artists You Follow
      .artsy-checkbox
        .artsy-checkbox--checkbox
          input( type='checkbox', id='for-sale' )
          label( for='for-sale' )
        label.artsy-checkbox--label( for='for-sale' ) Only For Sale
    #notifications-works
      .notifications-sub-header Recently Added Works
      //- Rendered client-side
      #notifications-feed
    //- #notifications-pins
    //-   //- Rendered client-side
    //- #notifications-feed
    //-   //- Rendered client-side
    //- #notifications-feed-spinner: .loading-spinner
