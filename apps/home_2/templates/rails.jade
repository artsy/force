if modules
  each module, index in modules
    - title = module.title
    - railId = module.key
    if (module.context)
      - viewAllUrl = viewHelpers.viewAllUrl(module)
    div(id="hpm-#{railId}-#{index}").abrv-container
      include ../../../components/artwork_brick_rail/templates/index