if feature && sale && sale.get('is_auction')
  //- Auction
  a( href= feature.href() )
    p
      | Work offered by
      br
      = feature.get('name')
    .avant-garde-jump-link
      | Go to auction

else if sale && sale.isAuctionPromo()
  //- Auction promo
  a( href= sale.href() )
    p
      | Work offered by
      = ' '
      if sale.has('profile')
        = sale.related().profile.displayName()
      else
        = sale.get('name')

    if sale.has('profile')
      p: img( src= sale.related().profile.iconImageUrl() )
    else
      .avant-garde-jump-link
        | Go to sale

else if feature && sale && !sale.get('is_auction')
  //- Sale
  a( href= feature.href() )
    p
      | Work offered by
      br
      = feature.get('name')
    .avant-garde-jump-link
      | Go to sale

else if fair && fair.get('has_full_feature') && fair.get('published')
  //- Fair
  a( href= fair.href() )
    p
      | Work offered by
      br
      = fair.get('name')
    .avant-garde-jump-link
      | Go to fair
