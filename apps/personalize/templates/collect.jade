i.icon-logotype.personalize-initial-logotype

header.personalize-header.avant-garde-header-center
  h2 60-Second Sign Up:
  h1 Do you buy art?

section.personalize-step
  nav.stacked-button-menu
    for level, i in state.get('levels')
      a.avant-garde-button-black(
        data-value=(state.get('levels').length - i)
      )= level
