var derequire = require('../');
var fs = require("fs");
var file = fs.readFileSync('./test/pouchdb.js',{encoding:'utf8'});
var soFar = 0;
var i = 0;
var start;
var transformed;
while(i++<10){
  console.log(i);
  start = new Date();
  transformed = derequire(file);
  soFar += ((new Date()) - start);
}
console.log(soFar);