//- Flexible Navigation
.team-channel-nav
  .responsive-layout-container
    .team-channel-nav__container
      .team-channel-nav__artsy-logo-square
        a(href='/')
          i.icon-logo
      .team-channel-nav__artsy-logo
        a(href='/')
          i.icon-logotype
      .team-channel-nav__name= channel.name
      .team-channel-nav__tagline= channel.tagline
      .team-channel-nav__links
        for link in channel.links
          if link.text && link.text.length > 0
            a(href=link.url)= link.text
        if channel.id === sd.GALLERY_INSIGHTS_CHANNEL
          a.team-channel-nav__apply(href="#{sd.APPLY_URL}") Apply
      a.team-channel-nav__hamburger.js-team-channel-toggle-hamburger
        .team-channel-nav__hamburger-icon
