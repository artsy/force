.overlay-container.save-controls-two-btn-container
  .overlay-button-save(
    data-state=(artwork.collection && artwork.collection.artworkCollection &&
                artwork.collection.artworkCollection.get('id') == 'saved-artwork' ? 'saved': null)
  )
  .save-controls-two-btn-add-to-collection
    include ../images/add_to_collection.svg