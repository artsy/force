h2.favorites2-empty-state-header
  | You haven't saved any works #{sd.CURRENT_PATH && sd.CURRENT_PATH.match('/collection/') && 'to this set'} yet.
  .favorites2-empty-state-header-cta
    | Add works to your favorites by clicking
    i.icon-heart
    | on artworks.
.large-garamond-header Find something you like
hr
ul.favorites2-empty-state.grid-4-up
  for link in featuredLinks
    li.grid-item
      a( href=link.get('href') )
        img( src=link.imageUrl('medium_rectangle') alt=link.get('title') )
        h3= link.get('title')