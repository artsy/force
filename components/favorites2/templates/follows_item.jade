.favorites2-follows-item(id=item.id)
  h3
    span.name
      a.avant-garde-button-text(href=item.clientUrl())= item.get('name')
    button.avant-garde-button-white.follow-button
  .artworks
    .loading-spinner
