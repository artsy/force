link( rel='apple-touch-icon', href=(sd.ASSET_PATH + 'icon-57.png') )
link( rel='apple-touch-icon', sizes='72x72', href=(sd.ASSET_PATH + 'icon-72.png') )
link( rel='apple-touch-icon', sizes='114x114', href=(sd.ASSET_PATH + 'icon-114.png') )
link( rel='icon', type='image/png', href=(sd.ASSET_PATH + 'favicon.ico') )
meta( name="msapplication-config" content=sd.ASSET_PATH + 'browserconfig.xml' )
meta( name="viewport" content="user-scalable=0, width=device-width" )
script.
  // If we're in an iframe, let the iframe know what path we're at
  window.parent.postMessage({ path: location.pathname + location.search }, location.origin);