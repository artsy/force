if user.get('name')
  a.user-nav-no-link( href="#" )= user.get('name')
  a.user-nav-profile-link.is-profile-text( href="/" + user.get('default_profile_id') ) Profile
else
  a.user-nav-no-link( href="#" ) account
a( href="/user/edit" ) Settings
if user.isAdmin()
  a( href=sd.ADMIN_URL ) Admin
if user.isAdmin() || user.get('has_partner_access')
  a( href=sd.CMS_URL ) CMS
a( href="/users/sign_out" ) Log out
