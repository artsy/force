- partner = artwork.related().partner

h1.iq-headline
  if partner.get('pre_qualify')
    | #{partner.get('name')} requests additional information before placing an inquiry.
    | You only have to do this once.
  else
    | To facilitate your introduction with the gallery,
    | we recommend to tell a little about yourself.

form.iq-form.stacked-form
  .iq-form-errors.js-form-errors
    //- Rendered separately

  label.avant-garde-form-label( for='profession' )
    | Profession <span>(#{partner.get('pre_qualify') ? 'Required' : 'Optional'})</span>
  input.bordered-input.js-iq-profession(
    name='profession'
    id='profession'
    value= user.get('profession')
    required= partner.get('pre_qualify')
  )

  label.avant-garde-form-label( for='location' )
    | Location <span>(#{partner.get('pre_qualify') ? 'Required' : 'Optional'})</span>
  .js-location-search
    //- Rendered separately

  label.avant-garde-form-label( for='phone' )
    | Phone Number <span>(#{partner.get('pre_qualify') ? 'Required' : 'Optional'})</span>
  input.bordered-input.js-iq-phone(
    name='phone'
    id='phone'
    type='tel'
    value= user.get('phone')
    required= partner.get('pre_qualify')
  )

  if user.isLoggedIn()
    .iq-checkbox.artsy-checkbox
      .artsy-checkbox--checkbox
        input(
          type='checkbox'
          id='share_follows'
          name='share_follows'
          checked= user.get('share_follows')
        )
        label( for='share_follows' )
      label.artsy-checkbox--label( for='share_follows' )
        | Share followed artists, categories, and galleries

  button.avant-garde-button-black.iq-next( type='submit' ) Next

if artwork.related().partner.get('pre_qualify')
  include ./partials/nevermind
