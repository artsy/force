figure.rich-link( id='link_' + attachment.id, data-attachment=attachment.id, style="width=" + attachment.maxWidthForWidth(imageWidth) + "px;" )
  .rich-player
    != attachment.get('oembed_json').html
