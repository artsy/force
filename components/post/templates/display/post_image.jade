figure.post-image( data-attachment=attachment.id )
  img( height=attachment.maxHeightForWidth(maxDimension), width=attachment.maxWidthForWidth(maxDimension), src=attachment.imageUrl('larger'), nopin="nopin" )
