describe 'Scroller', ->
  it 'triggers an event for when the scrollTop is below the element'

  it 'triggers an event for when the scrollTop is above the element'

  it 'triggers an event for when the scrollTop is enters the element'

  it 'triggers an event for when the scrollTop is leaves the element'

  it 'recalculates positions when images load'

  it 'recalculates positions on resize'
