if auction.isAuctionPromo()
  a.is-block.avant-garde-button-black.js-inquiry-button( data-id= artwork.id )
    | Contact #{auction.related().profile.displayName() || 'Auction House'}
else
  //- Only when the button is enabled use the black state.
  //- The disabled black state has been deemed unsuitable for this context...
  a.is-block.js-bid-button(
    data-id= artwork.id
    href= button.href
    class= button.enabled ? 'avant-garde-button-black' : 'avant-garde-button'
    disabled= !button.enabled
  )= button.label
