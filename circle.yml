machine:
  node:
    version: 6
deployment:
  staging:
    branch: master
    commands:
      - git remote add heroku git@heroku.com:force-staging.git
      - yarn deploy
dependencies:
  override:
    - yarn
test:
  override:
    - yarn test
